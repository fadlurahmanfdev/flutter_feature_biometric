import 'package:flutter_test/flutter_test.dart';

void main() {
  // final MarkPlatform initialPlatform = MarkPlatform.instance;
  //
  // test('$MethodChannelMark is the default instance', () {
  //   expect(initialPlatform, isInstanceOf<MethodChannelMark>());
  // });

  test('getPlatformVersion', () async {
    // Mark flutterFeatureBiometricPlugin = Mark();
    // MockMarkPlatform fakePlatform = MockMarkPlatform();
    // MarkPlatform.instance = fakePlatform;
    //
    // expect(await flutterFeatureBiometricPlugin.getPlatformVersion(), '42');
  });
}
