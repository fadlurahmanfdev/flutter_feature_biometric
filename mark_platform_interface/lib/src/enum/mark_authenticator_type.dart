/// Type of authenticator used in authentication requests.
enum MarkAuthenticatorType {
  /// Biometric authenticator such as fingerprint or face.
  biometric,

  /// Device credential authenticator such as PIN, pattern, or password.
  deviceCredential
}